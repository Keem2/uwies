//Form that user will submit to add schedule to database
import TextInput from "./textinput";
const MultiStepForm = () => {
   return <TextInput placeholder="Enter here..." label="Schedule Name" />;
};

export default MultiStepForm;
